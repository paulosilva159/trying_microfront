name: trying_microfront
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  microfront_monitor:
    path: packages/microfront_monitor
  microfront_basic_calculator:
    path: packages/microfront_basic_calculator
  cupertino_icons: ^1.0.2
  get_it: ^7.2.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
